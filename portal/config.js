/**
 * NeoNetrek Server Portal Configuration
 *
 * Server operators: edit these values to customize your server's portal page.
 */
window.NEONETREK_PORTAL = {
  // Server identity
  serverName: "",                // e.g. "US East Bronco"
  serverTagline: "",             // e.g. "Beginner friendly, all welcome"

  // Connection details
  serverHost: "",                // e.g. "netrek.example.com:2592"
  wsProxy: "",                   // e.g. "wss://netrek.example.com/ws"
  serverLocation: "",            // e.g. "New York, US"

  // Host information
  adminName: "",                 // e.g. "YourName"
  adminContact: "",              // e.g. "admin@example.com"

  // Message of the day (supports HTML)
  motd: "",

  // Server rules (array of strings)
  rules: [],
};
